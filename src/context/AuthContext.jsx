
import { createContext } from "react";

export const AuthContext = createContext(null);

// import { createContext } from "react";

// // 🔐 إنشاء الكونتكست فقط
// export const AuthContext = createContext(null);